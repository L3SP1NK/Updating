#!/bin/bash
BACKUP_HOME()
{
	rm -r ${HOME}/.bak/${USER}.tar
	tar --ignore-command-error -caf /tmp/${USER}.tar /home/lespink
	mv /tmp/${USER}.tar /home/${USER}/.bak/
}

if [[ ${EUID} -eq "0" ]]
	then
		echo "Run me as user!"
		exit 1
	else
		BACKUP_HOME
fi
