#!/bin/bash

## Method:

##		compress	->	stdout	->	split	->	stdin	->	compress	-.
##		^																 |
##		\<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-/

## Compression:
## level + update + fresh + link

## Split:
## XX bytes
