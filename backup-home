#!/bin/bash
BACKUP_HOME()
{
	which zip > /dev/null
	[ ${?} -ne "0" ] && exit 1
	rm -r ${HOME}/.bak/${USER}_home.zip
	zip -9 -r /tmp/${USER}_home /home/${USER}
	mv /tmp/${USER}_home..zip /home/${USER}/.bak/
}

if [[ ${EUID} -eq "0" ]]
	then
		echo "Run me as user!"
		exit 1
	else
		BACKUP_HOME
fi
